# OpenAI TTS 性能优化配置
# 将这些设置添加到你的 .env 文件中

# TTS 模型选择 (建议使用最新的模型)
# gpt-4o-mini-tts: 最新、最快、最智能的TTS模型
# tts-1: 较低延迟但质量稍低
# tts-1-hd: 高质量但延迟较高
TTS_MODEL=gpt-4o-mini-tts

# 语音选择 (新模型支持的语音)
# coral: 推荐的新语音，清晰自然
# alloy, ash, ballad, echo, fable, nova, onyx, sage, shimmer
TTS_VOICE=coral

# 输出格式 (影响延迟)
# wav: 最低延迟，推荐用于实时应用
# pcm: 类似WAV但更原始，延迟极低
# mp3: 默认格式，文件较小但有编码延迟
# opus: 适合流媒体，低延迟
TTS_FORMAT=wav

# 语音速度 (1.0=正常, 0.5-2.0)
# 稍微加快可以改善响应感
TTS_SPEED=1.1

# 文件保存模式 (调试用)
TTS_SAVE_MODE=false
